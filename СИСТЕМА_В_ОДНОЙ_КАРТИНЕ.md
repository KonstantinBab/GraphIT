# 📊 Система в одной картине

```
PDF (смета)
    ↓ [ЭТАП 1: ConstructionBillOCR]
    │ PDF → Изображения (400 DPI)
    │ Изображения → Ollama Vision OCR (devstral-24b)
    │ Выход OCR → Парсинг Markdown таблицы
    │ Построение иерархии (пропагация разделов)
    ↓
parsed.xlsx (~500-1000 строк)
    ↓ [ЭТАП 2: GESNClassifier]
    │ Загрузка: Qwen2.5-3B + QLoRA (4-bit, bf16)
    │ Пакетная обработка: ThreadPoolExecutor(workers=4)
    │ Для каждой строки: классификация 'work' или 'material'
    │ Переупорядочение колонок
    ↓
structured.xlsx
    ↓ [ЭТАП 3: Matcher]
    │ Кодирование BERTA (SentenceTransformer)
    │ Векторный поиск: Qdrant (top-8 кандидатов)
    │ LLM Селектор: Qwen2.5-7B + QLoRA (bf16)
    │ Выбор лучшего кандидата или 0 (без совпадения)
    ↓
matched.xlsx
    ↓ [ЭТАП 4: FinalAggregator]
    │ Группировка по: (Раздел, Код работы)
    │ Агрегация: sum(Кол-во), mean(уверенность)
    │ Форматирование & переупорядочение колонок
    ↓
final.xlsx (ГОТОВ К ИСПОЛЬЗОВАНИЮ)
    ↓ [UI: Gradio]
    └→ Скачивание / Просмотр в браузере
```

---

## 🔄 Подробное описание каждого этапа

### 📄 ЭТАП 1: Парсинг PDF (ConstructionBillOCR)

**Вход:** PDF файл (смета, 10+ страниц)

**Процесс:**
1. **PDF → Изображения** (pdf2image, 400 DPI)
   - Преобразование каждой страницы в растровое изображение
   - Параллельная обработка страниц (ThreadPoolExecutor)

2. **Предобработка изображений** (OpenCV)
   - RGBA → RGB конвертация
   - Grayscale преобразование
   - CLAHE контраст-усиление (улучшение читаемости)
   - Bilateral filtering (опционально)

3. **Ollama Vision OCR** (devstral-24b или qwen3-vl)
   - HTTP POST запрос к `localhost:11434/api/chat`
   - Извлечение текста из таблиц в Markdown формате
   - Время: 3-6 сек на страницу

4. **Парсинг Markdown таблицы**
   - Извлечение ячеек по `|` разделителю
   - Выделение жирного текста как разделов (**Раздел**)
   - Создание DataFrame с колонками: №п/п, Наименование, Ед. изм., Кол-во

5. **Построение иерархии**
   - Пропагация заголовков разделов вниз по строкам
   - Вычисление level (уровень в иерархии)
   - Сохранение parent_id и path для структуры

**Выход:** `parsed.xlsx` (~500-1000 строк)
- Колонки: №п/п, Раздел, Подраздел, Наименование, Ед. изм., Кол-во, page

---

### 🔧 ЭТАП 2: Структурирование (GESNClassifier)

**Вход:** `parsed.xlsx`

**Процесс:**
1. **Предобработка DataFrame**
   - Заполнение NaN пустыми строками
   - Удаление пробелов (strip)
   - Создание пустой колонки "Тип"

2. **Загрузка моделей** (GPU)
   - Базовая модель: Qwen2.5-3B-Instruct
   - LoRA адаптер: `classifier_llm/final/`
   - Квантование: 4-bit (NF4) на CUDA
   - Точность: bf16 (на CUDA), fp32 (на CPU)

3. **Классификация с LLM** (многопоточная)
   - ThreadPoolExecutor с 4 worker'ами
   - Для каждой строки: определить тип "Работа" или "Материал"
   - Время: 200-300 мс на строку

4. **Логика классификации**
   ```
   Если в ответе "работ" → 'work' (Работа)
   Если в ответе "материал" → 'material' (Материал)
   Иначе → 'material' (безопасный default)
   ```

5. **Переупорядочение колонок**
   - Целевой порядок: №п/п, Раздел, Подраздел, Наименование, Ед. изм., Кол-во, Тип

**Выход:** `structured.xlsx`
- Колонки: все из Stage 1 + колонка "Тип"
- Пример: Работа (380 строк), Материал (100 строк)

---

### 🔍 ЭТАП 3: Матчинг (Matcher)

**Вход:** `structured.xlsx`

**Процесс:**
1. **Кодирование с BERTA** (SentenceTransformer)
   - Модель: distiluse-base-multilingual-cased-v2
   - Размерность: 384-мерные эмбеддинги
   - Пакетная обработка: 16 образцов за раз
   - Время: ~100 запросов в сек

2. **Векторный поиск в Qdrant**
   - Коллекция: 'vikr' (~5000 объектов)
   - Поиск top-8 похожих кандидатов
   - Метрика: косинусное сходство (0.0 - 1.0)

3. **LLM Селектор** (Qwen2.5-7B + QLoRA, bf16)
   - Выбор лучшего кандидата из top-8
   - Вывод: номер кандидата (1-5) или 0 (нет совпадения)
   - Время: ~500 мс на запрос

4. **Получение кода**
   - Поиск кода работы в справочнике ВиКР
   - По выбору селектора

**Выход:** `matched.xlsx`
- Новые колонки: Смэтченная работа ВиКР, Код работы, Уверенность модели, Score эмбеддинга, Позиция в топ-5

**Пример результата:**
```
Запрос: "Монтаж буронабивных свай"
Код: 051201001
Наименование ВиКР: Монтаж буронабивных свай | Работы по устройству...
Уверенность: 0.92
Score эмбеддинга: 0.867
Позиция в топ-5: 1
```

---

### 📊 ЭТАП 4: Агрегация (FinalAggregator)

**Вход:** `matched.xlsx`

**Процесс:**
1. **Очистка данных**
   - Кол-во → числовой тип (NaN → 0)
   - Строковые поля → trim whitespace
   - Удаление строк без Код работы

2. **Группировка**
   - `group_key = Раздел + '|' + Код работы`
   - Извлечение имени ВиКР (до первого '|')

3. **Агрегация** (vectorized groupby.agg)
   - **Кол-во**: сумма (всё количество в группе)
   - **Уверенность модели**: среднее (качество совпадения)
   - **Score эмбеддинга**: среднее (схожесть текста)
   - **Ед. изм.**: первое непустое значение
   - **Наименование**: join уникальных значений

4. **Форматирование**
   - Переименование колонок
   - Переупорядочение: №п/п, Наименование ВиКР, Кол-во, Ед. изм., Раздел, Код, Позиция, Уверенность, Score, Наименования работ ВОР, Количество объединённых работ
   - Добавление метаданных

**Выход:** `final.xlsx` (итоговый результат)
- Уникальные строки по (Раздел, Код работы)
- Суммированные количества
- Метрики качества матчинга
- ~250 строк (сжатие с 500)

**Пример финального результата:**
```
№п/п: 1
Наименование ВиКР: Монтаж буронабивных свай
Кол-во: 60 (объединено из 3 строк)
Ед. изм.: шт
Раздел: 1 Фундамент
Код работы: 051201001
Уверенность модели: 0.92
Score эмбеддинга: 0.867
Наименования работ ВОР: Монтаж свай | Бетонирование | Вязка арм.
Количество объединённых работ: 3
```

---

## ⚙️ Технологический стек

| Компонент | Технология | Назначение |
|-----------|-----------|-----------|
| **Парсинг** | Ollama Vision (devstral-24b) | Извлечение текста из PDF таблиц |
| **Классификация** | QWEN2.5-3B + QLoRA (4-bit) | Определение типа (Работа/Материал) |
| **Кодирование** | BERTA (SentenceTransformer) | Векторное представление текста |
| **Поиск** | Qdrant Vector Database | Быстрый поиск похожих записей |
| **Выбор** | QWEN2.5-7B + QLoRA (bf16) | Выбор лучшего кандидата |
| **UI** | Gradio | Веб-интерфейс для пользователя |
| **Параллелизм** | ThreadPoolExecutor | Ускорение обработки |

---

## ⏱️ Время выполнения

```
ЭТАП 1 (Парсинг OCR):       45-60 сек  ⚠️ Bottleneck
ЭТАП 2 (Классификация):     30-60 сек
ЭТАП 3 (Матчинг):           20-40 сек
ЭТАП 4 (Агрегация):         <1 сек
─────────────────────────────────────
ВСЕГО:                       ~2-3 мин (для 10-страничной сметы)
```

---

## 🎯 Ключевые особенности

✅ **Параллельная обработка** (страницы, строки)
✅ **Пакетная обработка** (16-32 образца)
✅ **4-битное квантование** (экономия VRAM)
✅ **bf16 точность** (скорость инференса)
✅ **Очистка VRAM** после каждого этапа
✅ **Progress callbacks** для UI
✅ **Graceful error handling** (восстановление после ошибок)
✅ **Структурированное логирование** (с временными метками)

---

## 📌 Результат

**Входящие данные:** PDF смета (любой размер)
**Выходящие данные:** `final.xlsx` с:
- Уникальными кодами работ ВиКР
- Суммированными количествами
- Метриками качества матчинга
- Готовой к загрузке в GESN систему
